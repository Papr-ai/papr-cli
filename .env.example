# Papr Memory API Configuration (Required)
# Get your API key from https://dashboard.papr.ai
PAPR_MEMORY_API_KEY=enter-papr-memory-api-key-here

# Server URL (Optional — defaults to https://memory.papr.ai)
NEXT_PUBLIC_MEMORY_SERVER_URL=https://memory.papr.ai

# Fallback API key (Optional — used if PAPR_MEMORY_API_KEY is not set)
PAPR_API_KEY=enter-papr-memory-api-key-here
